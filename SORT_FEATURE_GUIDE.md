# 📊 Руководство по сортировке папок и заметок

> Как использовать новую функцию сортировки в Hidden Notes

## 🎯 Что это

Функция сортировки позволяет упорядочить папки и заметки по различным критериям:
- По названию (А→Я или Я→А)
- По дате создания (старые→новые или новые→старые)
- По дате изменения (старые→новые или новые→старые)

## 🚀 Как использовать

### 1. Открыть диалог сортировки

Кликните на кнопку **↕️ Сортировать** в header приложения.

### 2. Выбрать параметры

**Что сортировать:**
- 📁📝 Папки и заметки - сортирует все
- 📁 Только папки - сортирует только папки
- 📝 Только заметки - сортирует только заметки

**Сортировать по:**
- 🔤 Названию - алфавитный порядок
- 📅 Дате создания - когда создано
- 🕐 Дате изменения - когда последний раз редактировалось

**Порядок:**
- ⬆️ По возрастанию:
  - Для названия: А → Я
  - Для дат: Старые → Новые
- ⬇️ По убыванию:
  - Для названия: Я → А
  - Для дат: Новые → Старые

### 3. Применить

Нажмите кнопку **"Применить"** - элементы будут отсортированы мгновенно.

## 💡 Примеры использования

### Пример 1: Сортировка папок по названию

1. Открыть диалог сортировки
2. Выбрать "Только папки"
3. Выбрать "Названию"
4. Выбрать "А → Я"
5. Нажать "Применить"

**Результат**: Папки отсортированы в алфавитном порядке.

### Пример 2: Показать новые заметки сверху

1. Открыть диалог сортировки
2. Выбрать "Только заметки"
3. Выбрать "Дате изменения"
4. Выбрать "Новые → Старые"
5. Нажать "Применить"

**Результат**: Последние измененные заметки показываются первыми.

### Пример 3: Упорядочить все по дате создания

1. Открыть диалог сортировки
2. Выбрать "Папки и заметки"
3. Выбрать "Дате создания"
4. Выбрать "Старые → Новые"
5. Нажать "Применить"

**Результат**: Все элементы отсортированы от старых к новым.

## 🔄 Сброс настроек

Кнопка **"Сбросить"** возвращает настройки по умолчанию:
- Что сортировать: Папки и заметки
- Сортировать по: Дате изменения
- Порядок: Новые → Старые

## ⚙️ Технические детали

### Как работает сортировка

1. При нажатии "Применить" вызываются функции:
   - `sortFolders()` - для папок
   - `sortNotes()` - для заметок
   - `sortBoth()` - для всех

2. Элементы сортируются в памяти

3. Обновляется поле `order` для каждого элемента

4. Изменения сохраняются в Chrome Storage

5. UI обновляется автоматически

### Сохранение порядка

После сортировки порядок сохраняется:
- Новые папки/заметки добавляются в конец
- Drag & drop может изменить порядок
- Повторная сортировка перезаписывает порядок

### Локализация

Сортировка по названию учитывает язык:
```typescript
a.name.localeCompare(b.name, 'ru')
```

Это означает что:
- Русские буквы сортируются правильно (А, Б, В...)
- Английские буквы сортируются правильно (A, B, C...)
- Смешанные названия сортируются корректно

## 🎨 UI элементы

### Диалог сортировки

```
┌─────────────────────────────────┐
│ Сортировка                   [×]│
├─────────────────────────────────┤
│ Что сортировать:                │
│ ○ Папки и заметки               │
│ ○ Только папки                  │
│ ○ Только заметки                │
│                                 │
│ ─────────────────────────────── │
│                                 │
│ Сортировать по:                 │
│ ○ Названию                      │
│ ○ Дате создания                 │
│ ○ Дате изменения                │
│                                 │
│ ─────────────────────────────── │
│                                 │
│ Порядок:                        │
│ ○ А → Я / Старые → Новые        │
│ ○ Я → А / Новые → Старые        │
│                                 │
├─────────────────────────────────┤
│ [Сбросить]    [Отмена] [Применить]│
└─────────────────────────────────┘
```

### Уведомления

**Успех**:
```
✅ Отсортировано
Элементы успешно отсортированы
```

**Ошибка**:
```
❌ Ошибка сортировки
Не удалось отсортировать элементы
```

## 🔧 Горячие клавиши

В будущих версиях планируется добавить:
- `Ctrl+Shift+S` - открыть диалог сортировки
- `Ctrl+Shift+N` - сортировать по названию
- `Ctrl+Shift+D` - сортировать по дате

## 📝 Часто задаваемые вопросы

### Q: Сохраняется ли порядок после перезагрузки?
**A**: Да, порядок сохраняется в Chrome Storage и восстанавливается при следующем запуске.

### Q: Можно ли отменить сортировку?
**A**: Нет прямой кнопки "Отменить", но вы можете:
- Использовать drag & drop для ручного изменения порядка
- Применить другую сортировку

### Q: Влияет ли сортировка на архивные элементы?
**A**: Нет, сортируются только активные (не архивные) элементы.

### Q: Что происходит с новыми элементами после сортировки?
**A**: Новые элементы добавляются в конец списка. Чтобы включить их в сортировку, примените сортировку снова.

### Q: Можно ли сортировать элементы внутри папки?
**A**: Да, сортировка заметок работает для всех заметок, включая те что в папках.

## 🚀 Будущие улучшения

Планируется добавить:
- [ ] Автоматическая сортировка (всегда поддерживать порядок)
- [ ] Сохранение предпочтений сортировки
- [ ] Сортировка по цвету
- [ ] Сортировка по размеру заметки
- [ ] Множественная сортировка (сначала по папкам, потом по дате)

## 📊 Статистика

- **Компонентов**: 2 (SortDialog, RadioGroup)
- **Функций**: 3 (sortFolders, sortNotes, sortBoth)
- **Параметров**: 3 (target, field, order)
- **Вариантов сортировки**: 18 (3×3×2)
- **Языков**: 2 (русский, английский)

---

**Версия**: 1.0.0  
**Дата**: 2025-11-26  
**Статус**: ✅ Готово к использованию
