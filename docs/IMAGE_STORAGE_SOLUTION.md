# Решение проблемы переполнения хранилища для изображений

## 📊 Текущая ситуация

**Проблема**: Превышен лимит Chrome Storage (10 МБ) из-за изображений в base64.

**Причины**:
- Изображения конвертируются в base64 и хранятся прямо в HTML контенте заметок
- Base64 увеличивает размер на ~33%
- Нет сжатия изображений перед сохранением
- Все данные хранятся в `chrome.storage.local` (лимит 10 МБ)

## ✅ Решение: Многоуровневая система хранения изображений

### Уровень 1: Сжатие изображений (ОБЯЗАТЕЛЬНО)

**Что делаем**:
- Автоматическое сжатие всех изображений перед сохранением
- Конвертация в WebP формат (лучшее сжатие)
- Resize до максимум 1920px по большей стороне
- Качество 80% (баланс между размером и качеством)
- Дополнительное снижение качества, если размер всё ещё > 1MB

**Реализация**: `src/lib/image-compression.ts`

**Экономия**: 70-90% размера изображений

### Уровень 2: IndexedDB для больших изображений

**Что делаем**:
- Изображения > 500KB после сжатия сохраняем в IndexedDB
- В HTML храним только placeholder или ссылку
- IndexedDB имеет лимит ~50MB (vs 10MB в chrome.storage)
- IndexedDB НЕ удаляется при удалении расширения (бонус!)

**Реализация**: `src/lib/image-storage.ts`

**Экономия**: Освобождает место в chrome.storage для текста и метаданных

### Уровень 3: Мониторинг и предупреждения

**Что делаем**:
- Показываем предупреждение при приближении к лимиту (> 80%)
- Предлагаем очистку старых изображений
- Показываем статистику использования хранилища

## 🏗️ Архитектура решения

```
┌─────────────────────────────────────────┐
│      Пользователь загружает изображение │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│     compressImage() - сжатие WebP       │
│     - Resize до 1920px                  │
│     - Quality 80%                       │
│     - Доп. сжатие если > 1MB            │
└─────────────────────────────────────────┘
                    ↓
         ┌──────────┴──────────┐
         │                     │
    < 500KB              > 500KB
         │                     │
         ↓                     ↓
┌─────────────────┐  ┌──────────────────────┐
│ chrome.storage  │  │   IndexedDB         │
│ (в HTML заметки)│  │   (отдельное хранилище)│
└─────────────────┘  └──────────────────────┘
```

## 📝 Интеграция в код

### 1. Обновить Toolbar.tsx

```typescript
import { processImageForStorage } from '@/lib/image-storage';

// В обработчике загрузки изображения:
const handleImageUpload = async (event: React.ChangeEvent<HTMLInputElement>) => {
  const file = event.target.files?.[0];
  if (!file) return;
  
  // Получаем ID текущей заметки
  const noteId = currentNoteId; // из контекста/пропсов
  
  try {
    // Обрабатываем изображение (сжатие + сохранение)
    const { dataUrl, useIndexedDB, imageId } = await processImageForStorage(file, noteId);
    
    // Вставляем в редактор
    editor.chain().focus().setImage({ 
      src: dataUrl,
      alt: file.name,
      'data-image-id': imageId, // для IndexedDB
      'data-use-indexeddb': useIndexedDB ? 'true' : 'false',
    }).run();
  } catch (error) {
    console.error('Ошибка обработки изображения:', error);
    toast.error('Не удалось обработать изображение');
  }
};
```

### 2. Обновить TiptapEditor.tsx (drag & drop)

```typescript
import { processImageForStorage } from '@/lib/image-storage';

// В handleDrop:
const handleDrop = async (event: DragEvent) => {
  // ... существующий код ...
  
  const imageFile = imageFiles[0];
  if (!imageFile) return;
  
  try {
    const noteId = currentNoteId; // получить из контекста
    const { dataUrl, useIndexedDB, imageId } = await processImageForStorage(imageFile, noteId);
    
    editor.chain().focus().setImage({ 
      src: dataUrl,
      alt: imageFile.name,
      'data-image-id': imageId,
      'data-use-indexeddb': useIndexedDB ? 'true' : 'false',
    }).run();
  } catch (error) {
    console.error('Ошибка обработки изображения:', error);
  }
};
```

### 3. Создать компонент для загрузки изображений из IndexedDB

```typescript
// src/components/TiptapEditor/ImageLoader.tsx
import { useEffect, useState } from 'react';
import { getImageFromIndexedDB } from '@/lib/image-storage';

export const ImageLoader: React.FC<{ 
  src: string; 
  imageId?: string; 
  noteId: string;
}> = ({ src, imageId, noteId }) => {
  const [loadedSrc, setLoadedSrc] = useState(src);
  
  useEffect(() => {
    if (imageId && src.includes('Loading...')) {
      // Загружаем из IndexedDB
      getImageFromIndexedDB(imageId).then(dataUrl => {
        if (dataUrl) {
          setLoadedSrc(dataUrl);
        }
      });
    }
  }, [imageId, src, noteId]);
  
  return <img src={loadedSrc} alt="" />;
};
```

## 📈 Ожидаемые результаты

### До оптимизации:
- 1 изображение 5MB → 6.65MB в base64
- 2 таких изображения = 13.3MB → превышение лимита!

### После оптимизации:
- 1 изображение 5MB → сжатие до ~500KB (WebP, 1920px, 80%)
- 2 изображения = 1MB → помещается в chrome.storage
- Если изображение всё ещё > 500KB → в IndexedDB (50MB лимит)

**Экономия**: ~90% места в chrome.storage

## 🔄 Миграция существующих данных

Для существующих заметок с изображениями:

1. **Скрипт миграции**: При следующем открытии заметки с большими изображениями
   - Извлечь base64 из HTML
   - Проверить размер
   - Если > 500KB → переместить в IndexedDB
   - Обновить HTML с placeholder

2. **Постепенная миграция**: При редактировании заметки автоматически мигрировать изображения

## ⚠️ Важные замечания

1. **WebP поддержка**: Все современные браузеры поддерживают WebP (Chrome, Edge, Firefox, Safari 14+)
2. **IndexedDB доступность**: Работает только в window context (Side Panel), не в background
3. **Производительность**: Сжатие происходит асинхронно, может быть небольшая задержка при загрузке
4. **Обратная совместимость**: Старые изображения в base64 продолжают работать

### Уровень 3: Сохранение на диск пользователя (File System Access API)

**Что делаем**:
- Используем File System Access API для сохранения изображений на диск
- Пользователь может выбрать папку для постоянного хранения
- Изображения сохраняются в выбранной папке
- Поддержка постоянного доступа к папке (permission persistence)

**Реализация**: `src/lib/file-storage.ts`

**Преимущества**:
- Неограниченное хранилище (зависит от диска пользователя)
- Пользователь контролирует расположение файлов
- Можно использовать для бэкапов
- Работает офлайн

**Ограничения**:
- Требует Chrome 86+ (File System Access API)
- Пользователь должен выбрать папку вручную
- Не работает в Firefox/Safari (только Chrome/Edge)

## 🚀 Дальнейшие улучшения (v2.0)

1. **Облачное хранилище**: Для Pro версии - синхронизация изображений через облако
2. **Lazy loading**: Загружать изображения из IndexedDB только при просмотре
3. **Автоочистка**: Удалять неиспользуемые изображения (orphaned images)
4. **CDN**: Для Premium версии - хостинг изображений на CDN

## 📚 Связанные файлы

- `src/lib/image-compression.ts` - сжатие изображений
- `src/lib/image-storage.ts` - хранение в IndexedDB
- `src/lib/file-storage.ts` - сохранение на диск (File System Access API)
- `src/components/TiptapEditor/Toolbar.tsx` - загрузка изображений (обновлено)
- `src/components/TiptapEditor/TiptapEditor.tsx` - drag & drop (обновлено)
- `src/components/TiptapEditor/ImageLoader.tsx` - загрузка из IndexedDB
- `src/components/TiptapEditor/ImageStorageMenu.tsx` - меню сохранения на диск
- `src/components/Editor/EditorArea.tsx` - передача noteId (обновлено)
- `src/lib/storage.ts` - основное хранилище заметок

---

**Дата создания**: 2025-01-XX  
**Статус**: ✅ Готово к реализации  
**Приоритет**: 🔴 Критический

