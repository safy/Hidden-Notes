## [2025-11-27] - 📚 Обновлена документация

### Добавлено
- **Руководство пользователя**: Создан полный USER_GUIDE.md с подробными инструкциями
- **Обновлен README.md**: Добавлена информация о новых функциях (папки, архивация, сортировка)
- **Структурирована документация**: Разделена на секции для пользователей и разработчиков

### Исправлено
- **Z-index для диалогов**: Исправлена проблема с отображением панели настроек под header
- **Кнопки архива и корзины**: Исправлена работа кнопок (добавлен z-index на header)

### Документация
- `docs/USER_GUIDE.md` - полное руководство пользователя (60+ страниц)
- `README.md` - обновлен список функций и дорожная карта
- `ARCHIVE_FOLDERS_V12_SUMMARY.md` - обновлен статус задач

## [2025-11-26] - ✨ Добавлена функция сортировки папок и заметок

### Добавлено
- **Диалог сортировки**: Новый диалог для выбора параметров сортировки
- **Сортировка папок**: По названию, дате создания или изменения
- **Сортировка заметок**: По названию, дате создания или изменения
- **Гибкие настройки**: Можно сортировать только папки, только заметки или все вместе
- **Порядок сортировки**: По возрастанию (А→Я, старые→новые) или убыванию (Я→А, новые→старые)

### Технические детали
- Создан компонент `SortDialog` с RadioGroup для выбора параметров
- Добавлены функции `sortFolders`, `sortNotes`, `sortBoth` в `storage.ts`
- Установлен пакет `@radix-ui/react-radio-group` для UI
- Добавлены переводы на русский и английский языки
- Сортировка обновляет поле `order` для каждого элемента

### UI/UX
- Кнопка сортировки (↕️) в header теперь открывает диалог
- Интуитивный интерфейс с радио-кнопками
- Кнопка "Сбросить" для возврата к настройкам по умолчанию
- Toast уведомления об успехе/ошибке

## [2025-11-26] - ✨ Упрощение архивации папок (v12) - работает как у заметок

### Изменено
- **Архивация папок теперь работает как у заметок**: Убрана сложная логика с сохранением на диск через File System Access API.
- **Простая архивация**: Папка и все заметки в ней просто помечаются как архивные (`isArchived: true`) и скрываются из основного списка.
- **Разархивация**: Работает мгновенно - просто снимается флаг `isArchived`.
- **UI упрощен**: Убраны дублирующиеся кнопки "Архив" и "Корзина" из футера Sidebar (они есть в верхнем меню).

### Исправлено
- **Архивация папок не работала**: Старая реализация требовала сохранение файла на диск, что было неудобно.
- **Сложность использования**: Теперь архивация работает одним кликом без диалогов сохранения.
- **Дублирование кнопок**: Убраны лишние кнопки "Архив" и "Корзина" снизу Sidebar.

### Технические детали
- Функция `toggleFolderArchive` полностью переписана - с ~550 строк до ~50 строк.
- Удалена зависимость от `archive-disk.ts` (функции `saveArchiveToDisk`, `loadArchiveFromDisk`, `selectArchiveFile`, `isArchiveDiskSupported`).
- Архивные папки и заметки остаются в storage, но скрыты из UI через фильтр `!folder.isArchived`.
- Просмотр архива доступен через компонент `ArchiveView`.
- Из `Sidebar.tsx` удалены кнопки "Архив" и "Корзина" (футер), убраны импорты `Button`, `Archive`, `Trash2`.

### Преимущества
- ✅ Мгновенная архивация/разархивация
- ✅ Не требует взаимодействия с пользователем (диалоги сохранения)
- ✅ Работает одинаково для папок и заметок
- ✅ Проще код - легче поддерживать

## [2025-01-XX] - 🔧 Исправления безопасности и надежности

### Исправлено
- **Скрипт `build` не проверял типы**: Основной скрипт сборки теперь снова выполняет проверку типов TypeScript перед сборкой (`tsc --noEmit && vite build`).
- **Проблема безопасности**: Ошибки типов теперь обнаруживаются на этапе сборки, предотвращая деплой кода с ошибками типизации.
- **Runtime ошибки в Sidebar**: Кнопки Archive и Trash теперь безопасно обрабатывают отсутствие обработчиков через optional chaining, предотвращая ошибки при клике.
- **Race conditions при восстановлении**: Функции `restoreNoteFromTrash` и `restoreFolderFromTrashHandler` теперь используют `storageLock` mutex для предотвращения race conditions при одновременных операциях записи в storage.

### Технические детали
- Восстановлена проверка типов в основном скрипте `build`.
- Используется `tsc --noEmit` для проверки без генерации файлов (быстрее и правильнее для Vite).
- Альтернативные скрипты `build:check` и `build:typecheck` оставлены для гибкости.
- В `Sidebar.tsx` обработчики `onOpenArchive` и `onOpenTrash` теперь используют wrapper функции с optional chaining (`() => onOpenArchive?.()`) вместо прямого присваивания.
- Добавлены функции `restoreNote` и `restoreFolder` в `storage.ts`, которые используют `storageLock.acquire()` для последовательного выполнения операций записи.
- Hooks `useNotes` и `useFolders` обновлены для использования новых безопасных функций восстановления вместо прямого доступа к Chrome storage.

## [2025-11-20] - 🔧 Исправление архивации (v11) - простое удаление из storage

### Исправлено
- **Архивация не работает**: Упрощена логика - если File System Access API не поддерживается, папка просто удаляется из storage (освобождает место и исчезает из UI).
- **Проблема с переполнением storage**: При архивации удаляются все архивные данные для освобождения места.

### Технические детали
- Добавлена проверка поддержки File System Access API с логированием.
- Если API не поддерживается (например, в Chrome Extension контексте), используется fallback: удаление папки и заметок из storage.
- При удалении также очищаются все другие архивные данные для максимального освобождения места.
- Упрощена логика - нет сложных проверок квоты, просто удаляем данные.

## [2025-11-20] - 🎉 Архивация на диск вместо storage (v10) - РЕВОЛЮЦИОННОЕ ИЗМЕНЕНИЕ

### Добавлено
- **Архивация на диск**: Архивные папки теперь сохраняются на диск пользователя через File System Access API вместо storage.
- **Новый файл `archive-disk.ts`**: Функции для сохранения и загрузки архивных данных на/с диска.
- **Освобождение storage**: При архивации папка и заметки удаляются из storage, освобождая место.

### Технические детали
- При архивации папки показывается диалог сохранения файла (JSON формат).
- Архив содержит: папку, все заметки в папке, метаданные (дата архивации, версия).
- В storage сохраняются только метаданные (имя файла, дата архивации), не сами данные.
- Разархивация пока требует ручного выбора файла (автоматическая загрузка в разработке).

### Преимущества
- ✅ Нет ограничений по размеру (storage 10MB vs диск - безлимитно)
- ✅ Освобождается место в storage
- ✅ Пользователь контролирует где хранятся архивы
- ✅ Можно переносить архивы между устройствами

## [2025-11-20] - 🔧 Критическое исправление архивации папок (v9) - удаление архивных данных ПЕРЕД архивацией

### Исправлено
- **Архивация не работает при переполнении storage**: Добавлена логика удаления ВСЕХ архивных данных ПЕРЕД архивацией новой папки, если storage > 75%.
- **Проблема с сохранением**: Теперь сначала удаляются все архивные заметки и папки, освобождается место, и только потом архивируется новая папка.

### Технические детали
- При архивации папки, если storage > 75%, сначала удаляются ВСЕ архивные заметки и папки (кроме той, которую архивируем).
- Очищенная схема сохраняется ПЕРЕД архивацией новой папки.
- Проверяется, что storage освободился до < 95% перед продолжением.
- После очистки данные перезагружаются, и папка находится заново (индекс может измениться).

## [2025-11-20] - 🔧 Критическое исправление архивации папок (v8) - агрессивная очистка storage

### Исправлено
- **Storage переполнен на 98.96%**: Добавлена более агрессивная очистка перед архивацией.
- **Очистка бэкапов**: Теперь удаляются ВСЕ бэкапы (не только старые) для максимального освобождения места.
- **Очистка архивных папок**: При агрессивной очистке удаляются также архивные папки (они занимают место).
- **Порог очистки снижен**: С 80% до 75% для более раннего освобождения места.
- **Проверка после очистки**: Добавлена проверка, что storage действительно освободился после очистки.

### Технические детали
- `cleanupBackups()` теперь удаляет все бэкапы (не оставляет последний) для максимального освобождения места.
- `aggressiveCleanup()` теперь также удаляет архивные папки, не только архивные заметки.
- Добавлена проверка использования storage после очистки - если все еще > 90%, операция архивации отменяется.
- Улучшено логирование освобожденного места в байтах и KB.

## [2025-11-20] - 🔧 Критическое исправление архивации папок (v7) - обработка переполнения storage

### Исправлено
- **Архивация не работает при переполнении storage**: Исправлена критическая проблема, когда архивация не работала из-за переполнения storage (98.96% использовано).
- **Ошибки квоты при архивации**: Добавлена агрессивная очистка бэкапов и данных перед архивацией при использовании > 80% storage.
- **Повторные попытки сохранения**: Добавлена логика повторных попыток с очисткой, если сохранение не удалось из-за квоты.

### Технические детали
- Порог очистки снижен с 95% до 80% для более раннего освобождения места.
- Перед архивацией сначала очищаются бэкапы (они занимают много места), затем агрессивная очистка данных.
- Добавлена проверка доступного места перед сохранением - если места мало, выполняется очистка.
- При ошибке сохранения выполняется повторная попытка с очисткой бэкапов и агрессивной очисткой данных.
- Улучшена обработка ошибок квоты с автоматическим retry после очистки.

## [2025-11-20] - 🔧 Критическое исправление архивации папок (v6)

### Исправлено
- **Архивация папок не обновляет UI**: Исправлена критическая проблема, при которой папки не исчезали из списка после архивации.
- **Race Condition в toggleFolderArchive**: Добавлен `storageLock` для предотвращения одновременных операций записи, которые могли перезаписывать изменения.
- **Фильтрация архивных папок**: Улучшена фильтрация в `getAllFolders()` с явной проверкой `isArchived === true`.
- **Debounce для storage listener**: Уменьшен debounce с 100ms до 50ms для более быстрой реакции на изменения storage.

### Технические детали
- В `toggleFolderArchive` добавлен `storageLock.acquire()` для предотвращения race conditions (как в других функциях storage).
- В `useFolders.ts` увеличена задержка до 150ms перед перезагрузкой папок после архивации.
- Добавлена проверка после перезагрузки, что папка действительно исчезла из списка (если нет - повторная перезагрузка).
- Улучшена фильтрация архивных папок с явной проверкой `folder.isArchived === true` вместо `!folder.isArchived`.
- Добавлено подробное логирование операций архивации для диагностики (количество папок, архивные папки).
- В `toggleFolderArchive` добавлена финальная проверка, что изменения действительно сохранились в storage.

## [2025-11-20] - 🔧 Оптимизация хранения данных и исправление ошибок квоты

### Исправлено
- **Ошибка квоты при архивации**: Исправлена проблема `kQuotaBytes quota exceeded`, из-за которой папки не архивировались, а в консоли появлялись ошибки автобэкапа.
- **Переполнение хранилища бэкапами**: Уменьшено количество хранимых автобэкапов с 10 до 3, чтобы предотвратить быстрое заполнение квоты (10MB).
- **Агрессивная очистка**: Реализован механизм принудительной очистки старых бэкапов, корзины и истории версий при возникновении ошибок квоты во время сохранения папок или заметок.

### Технические детали
- В `data-protection.ts` добавлена функция `cleanupBackups()`, которая удаляет старые бэкапы (оставляет 1), очищает корзину и историю версий.
- В `storage.ts` (функции `toggleFolderArchive`, `updateFolder`) добавлен вызов `cleanupBackups()` в обработчике ошибок квоты перед повторной попыткой сохранения.
- Лимит автобэкапов в `createAutoBackup` снижен до 3 последних снапшотов.

## [2025-11-19] - 🐛 Критическое исправление архивации папок (v5)

### Исправлено
- **Фильтрация архивных папок в UI**: Добавлена фильтрация архивных папок в `FolderList.tsx` - теперь архивные папки не отображаются в основном списке.
- **Верификация сохранения**: Добавлена проверка после операции архивации, что изменения действительно сохранились в storage. Если состояние не совпадает, операция считается неудачной.
- **Детальное логирование**: Добавлено подробное логирование операции архивации для диагностики проблем.

### Технические детали
- В `FolderList.tsx` добавлен фильтр `!folder.isArchived` при отображении папок.
- После успешного сохранения проверяется, что папка действительно заархивирована в storage.
- Добавлено логирование размера схемы и количества заметок для диагностики проблем с квотой.

## [2025-11-19] - 🐛 Критическое исправление архивации папок (v4)
