## [2025-01-XX] - ✅ Реализация системы оптимизации изображений

### Добавлено

#### 🎯 Многоуровневая система хранения изображений
- **Сжатие изображений**: Автоматическое сжатие всех изображений перед сохранением
  - Конвертация в WebP формат (лучшее сжатие)
  - Resize до максимум 1920px по большей стороне
  - Качество 80% (баланс между размером и качеством)
  - Дополнительное снижение качества, если размер всё ещё > 1MB
  - Экономия: 70-90% размера изображений

- **IndexedDB для больших изображений**: 
  - Изображения > 500KB после сжатия сохраняются в IndexedDB
  - IndexedDB имеет лимит ~50MB (vs 10MB в chrome.storage)
  - IndexedDB НЕ удаляется при удалении расширения
  - Освобождает место в chrome.storage для текста и метаданных

- **Сохранение на диск пользователя**:
  - File System Access API для сохранения изображений на диск
  - Пользователь может выбрать папку для постоянного хранения
  - Поддержка постоянного доступа к папке (permission persistence)
  - Неограниченное хранилище (зависит от диска пользователя)

#### 📦 Новые файлы
- `src/lib/image-compression.ts` - утилиты для сжатия изображений
- `src/lib/image-storage.ts` - хранение больших изображений в IndexedDB
- `src/lib/file-storage.ts` - сохранение на диск через File System Access API
- `src/components/TiptapEditor/ImageLoader.tsx` - компонент для загрузки из IndexedDB
- `src/components/TiptapEditor/ImageStorageMenu.tsx` - меню для сохранения на диск
- `docs/IMAGE_STORAGE_SOLUTION.md` - полная документация решения

#### 🔧 Обновленные компоненты
- `src/components/TiptapEditor/Toolbar.tsx`:
  - Добавлен prop `noteId` для связи изображений с заметками
  - Интегрировано сжатие изображений при загрузке
  - Автоматическое сохранение больших изображений в IndexedDB

- `src/components/TiptapEditor/TiptapEditor.tsx`:
  - Добавлен prop `noteId` для связи изображений с заметками
  - Интегрировано сжатие при drag & drop
  - Автоматическое сохранение больших изображений в IndexedDB

- `src/components/Editor/EditorArea.tsx`:
  - Добавлен prop `noteId` и передача в Toolbar и TiptapEditor

- `src/components/NoteView/NoteView.tsx`:
  - Передача `noteId` в EditorArea

### Исправлено
- **Проблема переполнения хранилища**: Изображения теперь сжимаются и хранятся эффективно
- **Экономия места**: ~90% экономии места в chrome.storage для изображений

### Технические детали
- **Сжатие**: Canvas API + WebP формат
- **IndexedDB**: Отдельная база данных `HiddenNotesImages` для больших изображений
- **File System Access API**: Требует Chrome 86+ (совместимо с текущими требованиями)
- **Fallback**: Если сжатие не удалось, используется старый метод (base64)

### Ожидаемые результаты
- **До**: 1 изображение 5MB → 6.65MB в base64
- **После**: 1 изображение 5MB → ~500KB (WebP, сжатие)
- **Экономия**: ~90% места в chrome.storage

### Документация
- Создан `docs/IMAGE_STORAGE_SOLUTION.md` с полным описанием решения
- Обновлен `docs/changelog.md` с записью о реализации

---
