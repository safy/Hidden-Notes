# Hidden Notes - Cursor Rules

> Правила разработки Chrome Extension для защищенных заметок с визуальным скрытием конфиденциальной информации

## 🎯 О проекте

**Hidden Notes** - Chrome Extension (Manifest V3) для создания заметок с функцией визуального скрытия чувствительной информации.

**Стек**: React 18 + TypeScript + Tiptap + shadcn/ui + Tailwind CSS + Chrome Storage API

**Документация**: Все ключевые документы находятся в `/docs/`:
- `Project.md` - архитектура и технические решения
- `Tasktracker.md` - прогресс выполнения задач
- `Diary.md` - дневник разработки с техническими решениями
- `qa.md` - вопросы и уточнения по проекту
- `changelog.md` - хронология всех изменений

---

## 📝 Документирование процесса

### Обязательные файлы для обновления

1. **changelog.md** - обновляй при каждом значительном изменении:
   ```markdown
   ## [YYYY-MM-DD] - Краткое описание
   ### Добавлено
   - Новые функции и компоненты
   
   ### Изменено
   - Модификации существующего кода
   
   ### Исправлено
   - Исправленные баги
   
   ### Технические детали
   - Важные технические решения
   ```

2. **Tasktracker.md** - актуализируй статус задач:
   - Отмечай начало работы (⚪ → 🔵)
   - Отмечай завершение (🔵 → 🟢)
   - Обновляй прогресс фазы
   - Добавляй новые задачи при необходимости

3. **Diary.md** - добавляй записи при важных решениях:
   ```markdown
   ## 📅 [YYYY-MM-DD] | Краткое название
   
   ### 📊 Наблюдения
   - Что обнаружено/изучено
   
   ### ✅ Решения
   - Принятые технические решения с обоснованием
   
   ### ⚠️ Проблемы
   - Возникшие проблемы и их решения
   ```

4. **Project.md** - обновляй при изменении архитектуры:
   - Новые компоненты
   - Изменения в структуре
   - Обновления технологий

---

## ⚙️ Процесс разработки

### Workflow

1. **Перед началом задачи**:
   - Проверь текущую фазу в Tasktracker.md
   - Убедись что зависимости выполнены
   - Обнови статус задачи на 🔵 "В процессе"
   - Прочитай связанные разделы в Project.md

2. **Во время разработки**:
   - Следуй архитектурным решениям из Project.md
   - Проверяй qa.md на наличие связанных вопросов
   - Создавай коммиты согласно Conventional Commits
   - Запрашивай подтверждение перед значительными изменениями

3. **После завершения задачи**:
   - Обнови статус на 🟢 "Завершена"
   - Актуализируй changelog.md
   - Добавь запись в Diary.md (если есть важные решения)
   - Обнови Project.md (если изменилась архитектура)
   - Предоставь краткое резюме (не более 5 пунктов)

### Принципы

- **SOLID** - Single Responsibility для каждого компонента
- **KISS** - Простые решения предпочтительнее сложных
- **DRY** - Переиспользование через утилиты и хуки
- **Code Review** - Проверяй код на соответствие критериям приемки
- **Testing** - Пиши тесты для критичного функционала

---

## 💻 Стандарты кода

### TypeScript

```typescript
// ✅ ХОРОШО
interface Note {
  id: string;
  title: string;
  content: string;
  createdAt: number;
  updatedAt: number;
}

const createNote = (title: string): Note => {
  return {
    id: nanoid(),
    title,
    content: '',
    createdAt: Date.now(),
    updatedAt: Date.now(),
  };
};

// ❌ ПЛОХО - использование any
const createNote = (title: any): any => { ... }
```

**Правила**:
- Strict mode включен (`tsconfig.json`)
- Явная типизация всех функций и переменных
- Избегать `any`, использовать `unknown` при необходимости
- Interface для объектов, Type для union/intersection
- Descriptive naming: `isLoading`, `hasError`, `notesList`

### React Components

```typescript
// ✅ ХОРОШО - Functional component с типизацией
interface NoteItemProps {
  note: Note;
  isActive: boolean;
  onClick: (id: string) => void;
}

export const NoteItem: React.FC<NoteItemProps> = ({ note, isActive, onClick }) => {
  return (
    <div 
      className={cn("note-item", isActive && "active")}
      onClick={() => onClick(note.id)}
    >
      <h3>{note.title}</h3>
    </div>
  );
};

// ❌ ПЛОХО - Class component, props any
class NoteItem extends React.Component {
  render() {
    const { note } = this.props;
    ...
  }
}
```

**Правила**:
- Только Functional components
- Типизированные Props через interface
- Props деструктуризация
- Hooks для состояния и эффектов
- Именование: PascalCase для компонентов, camelCase для функций

### File Headers

```typescript
/**
 * @file: NoteList.tsx
 * @description: Компонент списка заметок с виртуализацией
 * @dependencies: useNotes hook, NoteItem component
 * @created: 2025-10-15
 */
```

Добавляй header при создании нового файла.

### CSS/Tailwind

```typescript
// ✅ ХОРОШО - Tailwind + cn helper
import { cn } from '@/lib/utils';

<div className={cn(
  "p-4 rounded-lg",
  isActive && "bg-primary",
  hasError && "border-red-500"
)} />

// ❌ ПЛОХО - Inline styles, string concatenation
<div style={{ padding: '16px' }} />
<div className={"p-4 " + (isActive ? "bg-primary" : "")} />
```

**Правила**:
- Tailwind utility classes приоритетны
- `cn()` для условных классов
- CSS modules для сложных компонентов
- Избегать inline styles
- CSS переменные для theme

### Git Commits

```bash
# ✅ ХОРОШО - Conventional Commits
feat(editor): add HiddenText extension
fix(storage): handle quota exceeded error
docs: update architecture in Project.md
style(toolbar): improve button spacing

# ❌ ПЛОХО
update files
fix bug
wip
```

**Формат**: `<type>(<scope>): <subject>`

**Types**: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`

---

## 🔍 Code Review Checklist

Перед завершением задачи проверь:

**Общее**:
- [ ] Код соответствует TypeScript strict mode
- [ ] Нет eslint ошибок/warnings
- [ ] Следование принципам SOLID, KISS, DRY
- [ ] Нет неиспользуемого кода
- [ ] Нет console.log (использовать logger)

**React**:
- [ ] Компоненты типизированы
- [ ] Props деструктурированы
- [ ] Нет unnecessary re-renders (проверено React DevTools)
- [ ] Hooks dependencies корректны

**Chrome Extension**:
- [ ] Соответствие Manifest V3
- [ ] Permissions минимальны
- [ ] Storage квота отслеживается
- [ ] Нет eval() или new Function()

**Тестирование**:
- [ ] Unit тесты для утилит (coverage > 80%)
- [ ] Integration тесты для key flows
- [ ] Manual тест в Chrome

**Документация**:
- [ ] changelog.md обновлен
- [ ] Tasktracker.md статус актуален
- [ ] Diary.md содержит важные решения (если есть)
- [ ] Project.md обновлен (если изменилась архитектура)

---

## 🎯 Специфика проекта Hidden Notes

### Chrome Extension

- **Manifest V3** - используй современные API
- **Side Panel** - требует Chrome 114+
- **Service Worker** - вместо background pages
- **Storage API** - Chrome Storage Local (10MB лимит)

### Tiptap/ProseMirror

```typescript
// Создание кастомного расширения
export const HiddenText = Mark.create({
  name: 'hiddenText',
  addAttributes() { ... },
  parseHTML() { ... },
  renderHTML() { ... },
  addCommands() { ... },
});

// Использование в редакторе
const editor = useEditor({
  extensions: [
    StarterKit,
    HiddenText, // Кастомное расширение
  ],
});
```

### HiddenText Feature

**Механизм**:
1. Пользователь выделяет текст
2. ПКМ → "Скрыть текст"
3. Применяется `hiddenText` Mark
4. CSS эффект "шума" скрывает текст визуально
5. Alt + hover = временное раскрытие
6. Ctrl + click = копирование без раскрытия

**Важно**: Это ТОЛЬКО визуальное скрытие, не шифрование!

---

## 🚨 Важные ограничения

### Chrome Storage

- **Квота**: 10MB
- **Мониторинг**: Предупреждать при > 80%
- **Оптимизация**: Сжимать изображения (WebP, 80% quality)

### Performance Targets

| Метрика | Целевое значение |
|---------|------------------|
| Side Panel загрузка | < 500ms |
| Ввод текста | < 16ms (60 FPS) |
| Сохранение заметки | < 100ms |
| Bundle size | < 1MB |
| Поиск (1000 notes) | < 50ms |

### Browser Support

- **Chrome**: 114+ (Side Panel API)
- **Edge**: 114+ (Chromium)
- **Brave**: 114+ (Chromium)
- Firefox/Safari: НЕ поддерживаются (используют разные API)

---

## 📚 Ресурсы

### Документация
- [Tiptap Docs](https://tiptap.dev/docs)
- [Chrome Extension Docs](https://developer.chrome.com/docs/extensions/)
- [shadcn/ui](https://ui.shadcn.com/)

### Проектная документация
- `docs/Project.md` - архитектура
- `docs/Tasktracker.md` - задачи
- `docs/Diary.md` - технические решения
- `docs/qa.md` - вопросы и ответы

---

## 📞 Коммуникация

1. **Неясные требования** → Добавь вопрос в qa.md
2. **Несколько вариантов** → Опиши pros/cons каждого
3. **Большая задача** → Предложи разбить на подзадачи
4. **Конец сессии** → Краткий отчет + планы на следующую сессию

---

## ✅ Правило первой операции

**ПЕРЕД любой операцией по разработке**:

1. Прочитай текущую задачу в Tasktracker.md
2. Проверь связанные решения в Project.md
3. Просмотри qa.md на наличие связанных вопросов
4. Убедись в наличии всех зависимостей

**ПОСЛЕ завершения операции**:

1. Обнови статус в Tasktracker.md
2. Актуализируй changelog.md
3. Добавь запись в Diary.md (при важных решениях)
4. Предоставь краткое резюме

---

**При любых изменениях: документация → код → тесты → документация**

Это обеспечит консистентность и позволит избежать потери контекста.